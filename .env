APP_URL=https://n8n-kapusta2.magictext.online
# Задержка этапа обработки документов на фронтенде, в секундах (0 — без задержки).
# Изменение применяется после `docker compose up -d` (пересоздание контейнера).
PROCESSING_DELAY_SECONDS=10
